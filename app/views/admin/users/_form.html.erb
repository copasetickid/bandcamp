<%= simple_form_for [:admin, user] do |f| %>
	<%= f.input :email %>
	<%= f.input :password %>
	
	<%= f.input :role, label: "Assign a Role" do %>
  		<%= f.select :role, User.roles.keys.map {|role| [role.titleize, role]} %>
	<% end %>
	<%= f.button :submit, class: "btn btn-primary" %>
<% end %>