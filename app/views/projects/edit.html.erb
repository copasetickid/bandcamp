<div class="container">
	<div class="page-header">
		<h1>Edit Project
	      <small><%= @project.name %></small>
	  </h1>
	</div>
	<%= render "form", project: @project %>

	<%= render "collaborators", project: @project, user: @available_users %>
</div>
